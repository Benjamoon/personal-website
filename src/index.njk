---
layout: base.njk
title: Eleventy Web Starter - Eleventy, esbuild, Tailwind and PostCSS
description: Eleventy Web Starter is a lightweight Eleventy boilerplate utilising esbuild, Tailwind CSS and Post CSS
---

<main id="main" class="p-6 md:p-12 lg:p-16 border-t-8 border-gray-200">
    <div class="max-w-xl">
        <h1 class="font-bold text-xl mb-2">{{site.title}} <span class="font-light text-gray-500">v{{ pkg.version }}</span></h1>
        <p>{{ site.description }}</p>
        <br>
        <ul class="max-w-md space-y-1 text-gray-500 list-disc list-inside dark:text-gray-400">

            {% for item in projects %}
                <li>{{item.title}}</li>
            {% endfor %}

        </ul>

        <footer class="text-xs text-gray-500 mt-12 border-t border-gray-100 pt-6">
            <p>{{ site.author }}</p>
        </footer>
    </div>
</main>